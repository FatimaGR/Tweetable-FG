<!DOCTYPE html>
<html>
  <head>
    <title>TweetableIndividual</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class = "navbar container-sm">
      <% if logged_in? %>
        <p class = "navbar-message "> Welcome, <%= current_user.username %> !</p>
        <p><%= link_to "edit profile", "/profile", class: "navbar-text" %></p>
        <%= link_to "logout", sessions_path, data: { turbo_method: :delete }, class: "button button--sm button--ghost" %>
      <%else%>
        <%= link_to "login", "/login", class: "button button--sm button--ghost" %>
      <%end%>
    </div>
    <%= yield %>
  </body>
</html>
